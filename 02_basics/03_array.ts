const superHeros: string[] = [];
// const heroPowers: number[] = [];
const heroPowers: Array<number> = []; // same working as previous, just different syntax

type User = {
  name: String;
  isActive: boolean;
};

const allUsers: User[] = [];

// Array in array
const MLModels: number[][] = [
    [255,255,255],
    []
]

superHeros.push('spiderman');
heroPowers.push(2);

allUsers.push({ name: 'rahul', isActive: false });


export {}